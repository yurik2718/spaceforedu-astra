---
// Astro i18n (redirectToDefaultLocale: true) требует корневой index как якорь
// для авто-редиректа на /es/. Эмитится meta-refresh index.html — это фолбэк
// для локального preview и любых нестандартных хостингов.
// На Cloudflare Pages public/_redirects со 301 обрабатывается раньше статики,
// так что пользователь мгновенно улетает на /es/, не видя этой страницы.
---
